[profile.default]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"
via_ir = true
src = "src"
out = "out"
script = "script"
libs = ["lib"]
test = "test"

[fuzz]
runs = 256
