// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for GetUserAddressesFunction
 */
export interface GetUserAddressesFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/lambda-functions/get-user-addresses/get-user-addresses.
 */
export class GetUserAddressesFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: GetUserAddressesFunctionProps) {
    super(scope, id, {
      description: 'src/lambda-functions/get-user-addresses/get-user-addresses.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs22.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/lambda-functions/get-user-addresses/get-user-addresses.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}